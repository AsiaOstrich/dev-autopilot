{
  "name": "@devap/adapter-vibeops",
  "version": "0.5.1",
  "description": "VibeOps adapter for devap — integrates VibeOps 7+1 agents via HTTP API",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@devap/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "license": "MIT"
}
