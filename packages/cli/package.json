{
  "name": "@devap/cli",
  "version": "0.1.0",
  "description": "CLI entry point for devap",
  "type": "module",
  "bin": {
    "devap": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@devap/core": "workspace:*",
    "@devap/adapter-claude": "workspace:*",
    "@devap/adapter-opencode": "workspace:*",
    "@devap/adapter-cli": "workspace:*",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "license": "Apache-2.0"
}
